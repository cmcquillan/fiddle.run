import { TransformBuilderDirective } from './transform-builder.directive';

describe('TransformBuilderDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformBuilderDirective();
    expect(directive).toBeTruthy();
  });
});
